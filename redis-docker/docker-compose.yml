services:
  redis-stack:
    image: redis/redis-stack:latest
    container_name: wqi_redis_stack
    ports:
      - "6379:6379"   # Redis
      - "8001:8001"   # RedisInsight UI
    volumes:
      - redis_data:/data
    restart: unless-stopped

volumes:
  redis_data: